# Backend Despachos — código fuente

La documentación completa del repositorio (arquitectura, CI/CD, volúmenes y despliegue) está en el **[README de la raíz](../README.md)**.

Para ejecutar en local:

```bash
cd Springboot-API-REST-DESPACHO
docker compose up -d --build
```
